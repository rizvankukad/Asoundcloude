/*
* Application JS file
*
* Application Main module where application get started
* load basic module like Express Engine, Configurations,
* Database module, Static Contents & Routes
*
* Author: Rizwan Kukad
* Date: 4-January-2014 12:42 AM IST
*/

var express = require('express'), /* Loading main Express Engine */
    app = express(),
    path = require('path'),
    favicon = require('serve-favicon'),
    logger = require('morgan'),
    cookieParser = require('cookie-parser'),
    bodyParser = require('body-parser'),
    routes = require('./routes/index'), /* Loading Routes */
    config = require('./configuration/config')(app), /* Loading Application Configurations */
    dbconnection = require('./code/database/main').connect(app); /* Loading Mongoose for connecting mongodb */

/* View Engine & Static Content setup*/
app.set('views', path.join(__dirname, 'views'));
app.set('view engine', 'jade');

// uncomment after placing your favicon in /public
//app.use(favicon(__dirname + '/public/favicon.ico'));

app.use(logger('dev'));
app.use(bodyParser.json());
app.use(bodyParser.urlencoded({ extended: false }));
app.use(cookieParser());
app.use(express.static(path.join(__dirname, 'public')));

app.use('/', routes); /* Routes Setup */

// catch 404 and forward to error handler
app.use(function(req, res, next) {
    var err = new Error('Not Found');
    err.status = 404;
    next(err);
});

// error handlers

// development error handler
// will print stacktrace
if (app.get('env') === 'development') {
    app.use(function(err, req, res, next) {
        res.status(err.status || 500);
        res.render('error', {
            message: err.message,
            error: err
        });
    });
}

// production error handler
// no stacktraces leaked to user
app.use(function(err, req, res, next) {
    res.status(err.status || 500);
    res.render('error', {
        message: err.message,
        error: {}
    });
});


module.exports = app;
